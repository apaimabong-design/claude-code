name: Leak Test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show token (masked but still exists)
        run: echo "Token length: ${#GITHUB_TOKEN}"
        
      - name: Test external API
        run: |
          curl -H "Authorization: Bearer $GITHUB_TOKEN" \
               https://api.github.com/user
